// export default function () {
//   // default로 나가는 함수는 이름을 지정하지 않아도 문제가 없음
//   return Math.floor(Math.random() * 10)
// }
export function random() {
  // default로 나가는 함수는 이름을 지정하지 않아도 문제가 없음
  return Math.floor(Math.random() * 10)
}
export const user = {
  name: 'HEROPY',
  age: 85
}
export default 123